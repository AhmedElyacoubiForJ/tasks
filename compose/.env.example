# ----------------------------------------
# 1. Admin-User für den PostgreSQL-Container selbst
# ----------------------------------------
# Dieser Superuser wird vom PostgreSQL-Image automatisch angelegt.
# Wir benutzen ihn im Init-Skript, um unseren eigentlichen App-User anzulegen.

POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=postgres

# ----------------------------------------
# 2. Eigener, eingeschränkter User für deine Spring Boot App
# ----------------------------------------
# Diese Variablen werden von unserem Init-Skript gelesen und verwendet.
# **Wichtig:** Der `POSTGRES_USER` ist ein temporärer Superuser.
# Der `APP_DB_USER` ist der, den deine Spring Boot Anwendung später
# in ihrer `application-container-dev.properties` verwenden wird.

APP_DB_USER=dev_user
APP_DB_PASSWORD=dev_pass
DB_NAME=tasks_dev_db

# ----------------------------------------
# 3. Container-Namen & Ports
# ----------------------------------------
# Diese Namen werden von den Skripten verwendet, um Container zu starten, zu stoppen und zu überwachen.

DB_CONTAINER=postgres-dev
APP_CONTAINER=tasks-app
DB_PORT=5432
APP_PORT=8080

# ----------------------------------------
# 4. Docker-Image für die Datenbank
# ----------------------------------------
# Du kannst hier z.B. `postgres:16`, `postgres:alpine` oder ein eigenes Image verwenden.

DB_IMAGE=postgres:alpine

# ----------------------------------------
# 5. Volume-Name für persistente Daten
# ----------------------------------------

VOLUME=pgdata-dev

# ----------------------------------------
# 6. Spring Boot Profil (optional)
# ----------------------------------------

SPRING_PROFILES_ACTIVE=container-dev
