# src/main/resources/config/application-local-docker-db.yml
spring:
  # 🗄️ Datenbank-Konfiguration (PostgreSQL)
  # Variablen aus .env.local-docker-db
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:tasks_dev_db}
    username: ${APP_DB_USER:dev_user}
    password: ${APP_DB_PASSWORD:dev_password}

  # 🧬 JPA & Hibernate
  jpa:
    hibernate:
      ddl-auto: update  # 🚫 Keine automatische Schema-Generierung
    properties:
      hibernate:
        format_sql: true  # 📄 Formatierte SQL-Ausgabe

  # 🚫 H2-Konsole deaktiviert
  h2:
    console:
      enabled: false
  devtools:
    livereload:
      enabled: false  # 🚫 LiveReload deaktivieren
    restart:
      enabled: false  # 🚫 Auto-Restart deaktivieren

# 📋 Logging
logging:
  interceptor:
    enabled: console
